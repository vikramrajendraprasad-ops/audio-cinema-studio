name: audio_cinema_studio
description: Audio Cinema Studio – External FFmpeg-powered audio processor
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  file_picker: ^6.1.1
  shared_preferences: ^2.2.2
  path: ^1.9.0        # ✅ REQUIRED

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
