name: audio_cinema_studio
description: Audio Cinema Studio
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  file_picker: ^6.1.1

flutter:
  uses-material-design: true

  dependencies:
  flutter:
    sdk: flutter

  file_picker: ^6.1.1
  shared_preferences: ^2.2.2
